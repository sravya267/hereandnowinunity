name: Deploy to Cloud Run

on:
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        run: |
          gcloud builds submit \
            --tag us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/astro-chart/app:${{ github.sha }} \
            --project ${{ secrets.GCP_PROJECT_ID }} \
            --quiet
          gcloud run deploy astro-chart \
            --image us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/astro-chart/app:${{ github.sha }} \
            --region us-central1 \
            --allow-unauthenticated \
            --port 8080 \
            --memory 1Gi \
            --project ${{ secrets.GCP_PROJECT_ID }} \
            --quiet

      - name: Show service URL
        run: |
          gcloud run services describe astro-chart \
            --region us-central1 \
            --project ${{ secrets.GCP_PROJECT_ID }} \
            --format 'value(status.url)'
